An interactive JSON Tree Visualizer built with React and React Flow.
It allows users to upload or input JSON data, visualize it as a hierarchical tree structure, and download the visualization as an image.
The app supports dark mode, zooming, and is designed to fit within a micro-frontend architecture.

🚀 Features

 Visualize JSON Data — Convert any valid JSON into an interactive tree diagram.

 Toolbar Controls — Zoom In, Zoom Out, Fit View, and Download Tree options.

 Download as Image — Save the visualized tree as a .png snapshot.

 Dark Mode Support — Auto adapts to the global app theme.



##  Installation & Setup

1. **Clone the repository**
   ```bash
   git clone https://github.com/<your-username>/json-tree-visualizer.git
   cd json-tree-visualizer
Install dependencies

Install dependencies
npm install


Run the development server
npm run dev

Open in browser
http://localhost:5173
